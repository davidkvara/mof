{
  "name": "mof",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html && workbox generateSW"
  },
  "devDependencies": {
    "parcel-bundler": "^1.9.6"
  }
}
